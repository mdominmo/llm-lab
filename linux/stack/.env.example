# Copiar a `linux/stack/.env` y rellenar. `.env` está en .gitignore.
#
# Clave maestra de LiteLLM: la que usan el PC y Open WebUI para autenticarse.
# Generar una: openssl rand -hex 24
LITELLM_MASTER_KEY=sk-...

# IP de tailnet del Mac (la misma que en /etc/hosts).
MACBOOK_IP=100.x.y.z
