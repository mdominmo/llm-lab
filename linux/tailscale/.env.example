# Copiar a `linux/tailscale/.env`. Solo hace falta en el PRIMER arranque:
# despues la identidad del nodo vive en ./tailscale-state y la clave sobra.
# Generar en: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-...
